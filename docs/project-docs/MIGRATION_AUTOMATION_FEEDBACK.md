# Migration Automation Feedback

This file stores run summaries generated by the automation loop.
Keep only the last 3 sessions. Older entries are archived in git history.

## Notes
- If text corruption is detected, regenerate this file and rerun automation.
- Runtime JSON logs are written under `automation/logs/`.
- The compact machine-readable entry point for the next session is `automation/next-session-manifest.json`.

---

## Run Summary — 2026-03-13

### Termination reason
COMPLETION — exit code 0, all steps PASS (with skip flags)

### Run stats
- Total runs: 3
- pass_history: [0, 2, 6]
- Final PASS/FAIL/SKIP: 6/0/0
- Run 1: FAIL — UTF8_MOJIBAKE_DETECTED (pipeline blocked at step 1)
- Run 2: FAIL — TOMCAT_NOT_READY (2/3 steps passed)
- Run 3: PASS — exit 0 (6/6 steps, skip flags: --skip-tomcat-check --skip-session-contract-check --skip-frontend-compile-check)

### Patterns observed
- **UTF8_MOJIBAKE_DETECTED**: `src/main/java/com/rays/app/util/file/CompressionUtil.java` line 27 — EUC-KR Javadoc 깨짐. 영문 주석으로 교체.
- **bootstrap.sh 경로 버그**: 상태 문서를 `$PROJECT_ROOT/`만 탐색, `migration_tool/` 하위에 있어 `phase=Unknown` 출력. fallback 탐색 추가.
- **validate-skill-integration.sh find 크래시**: `$PROJECT_ROOT/docs/project-docs/`가 없으면 `set -o pipefail`로 스크립트 전체 중단. 디렉토리 사전 생성으로 해결.
- **Phase 0 설정 미완**: `dispatcher-servlet.xml` SPA 라우팅, `SpaForwardController.java`, `ViewController.java` ui 제외 패턴 — 전부 이번 세션에 생성.
- **CRA 진입점 없음**: `public/index.html`, `src/index.js` 미생성 → `npm run build` 불가. 다음 세션에서 생성 필요.

### Documents updated
- `AGENTS.md`: WSL/Tomcat 우회 플래그, bootstrap.sh 경로 규약, validate docs/project-docs 사전 생성 요건 추가
- `LATEST_STATE.md`: Phase 0 완료 항목 반영, 마지막 실행 결과 기록
- `TASK_BOARD.md`: Phase 0 완료 5개 체크, CRA 진입점 작업 분리

### Unresolved issues
- CRA 진입점 파일 없음 (`public/index.html`, `src/index.js`) → 자동화에서 직접 생성 예정
- Tomcat WSL 접근: Windows 호스트 IP(`172.26.208.1:8080`) 경유 시 접근 가능 — `--tomcat-base-url` 플래그 활용 예정
